name: Ascii Workflow

on: [push]

jobs:
  ascii_job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: Execute shell script
        run: echo "Script is not available"
  ascii_job1:
    needs: ascii_job
    runs-on: ubuntu-latest
    steps:
      - name: verigy the job
        run: echo "run the job" > data.txt
      - name: display the data
        run: ls -l

